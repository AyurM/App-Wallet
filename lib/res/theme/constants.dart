import 'package:flutter/material.dart';

const kBottomNavBarHeight = 80.0;
const kAppBarBottomHeight = 30.0;
const kExtendedToolbarHeight = 64.0;
const kHorizontalPadding20 = EdgeInsets.symmetric(horizontal: 20);
const kHorizontalPadding10 = EdgeInsets.symmetric(horizontal: 10);
